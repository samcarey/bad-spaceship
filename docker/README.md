This is an attempt at running Bevy from a Docker container,
which has not been successful yet.
I'm keeping this as a reference of the steps I've figured out so far.

Currently, I'm stuck because when a game tries to run,
wgpu complains that it can't find a GPU,
which apparently docker container's don't necessarily have????

Maybe I'll figure this out later...
