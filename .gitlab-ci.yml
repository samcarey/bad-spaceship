image: samcarey/nobo.games-docker-ci

pages:
  stage: deploy
  script:
    - cd bevy && cargo make build-web && cd ..
    - mkdocs build --strict --verbose --site-dir public --config-file mkdocs.yml
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "ci"'
      # changes:
      # - mkdocs.yml
      # - docs/*

