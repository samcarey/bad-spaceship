{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "cd ${workspaceFolder}/bevy ; cargo run --release",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build (windows)",
            "type": "shell",
            "command": "cd ${workspaceFolder}/bevy ; cargo build"
        },
        {
            "label": "build (unix)",
            "type": "shell",
            "command": "cd ${workspaceFolder}/bevy && cargo build"
        }
    ]
}